#include "main.h"

/**
 * mul - fucntion that culculate the multiplication of two numbers
 * @a: the first parameter and it's integer
 * @b: the second parameter and it's integer
 * Return: the resut of the mulitplication
 */
int mul(int a, int b)
{
	return (a * b);
}
