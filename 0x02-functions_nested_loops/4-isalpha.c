/**
 * _isalpha - Tell us if a charracter is in lower case
 *
 * @i: is an integer parameter
 * Return: 1 is true and 0 otherwise
 */
int _isalpha(int i)
{
	if (i >= 65 && i <= 118)
		return (1);
	return (0);

}
