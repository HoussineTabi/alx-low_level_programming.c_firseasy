/**
 * _islower - Tell us if a charracter is in lower case
 *
 * @i: is an integer parameter
 * Return: 1 is true and 0 otherwise
 */
int _islower(int i)
{
	if (i >= 98 && i <= 118)
		return (1);
	return (0);

}
